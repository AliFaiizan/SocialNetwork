{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is the backend of social network",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "server" : "nodemon index.js --ignore tests",
    "frontend" : "npm run start --prefix frontend",
    "dev" : "concurrently \"npm run server \" \"npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AliFaizan",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "concurrently": "^7.3.0",
    "nodemon": "^2.0.19"
  }
}
